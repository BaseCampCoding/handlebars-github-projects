# Handlebars for Github Projects

Use the code you wrote to recreate Github's repository projects page
to create the template(s) required to build the page based on the
`PAGE_DATA` object provided in `data.js`.
